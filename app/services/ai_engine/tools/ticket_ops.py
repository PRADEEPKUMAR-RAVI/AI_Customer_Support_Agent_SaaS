"""ticket_ops — placeholder. Ticket transitions are code-enforced by the engine via the
whitelist/guarded-CAS primitive, so they are deliberately NOT exposed to the model as a tool.
person-3 owns M5; the engine calls ticket_service directly."""
